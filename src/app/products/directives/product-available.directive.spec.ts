import { ProductAvailableDirective } from './product-available.directive';

describe('ProductAvailableDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductAvailableDirective();
    expect(directive).toBeTruthy();
  });
});

import { HostInspectDirective } from './host-inspect.directive';

describe('HostInspectDirective', () => {
  it('should create an instance', () => {
    const directive = new HostInspectDirective();
    expect(directive).toBeTruthy();
  });
});
